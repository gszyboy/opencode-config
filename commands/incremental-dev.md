---
description: 在已有项目上进行增量开发（50行平衡模式）
agent: build
---

## 任务：$ARGUMENTS

@~/.config/opencode/docs/existing-project-rules.md

## 介入级别确认

根据改动范围选择介入级别：

| 级别 | 适用场景 | 改动限制 |
|------|----------|----------|
| L3 小改 | 改一个函数/一行配置 | ≤10行，逐次确认 |
| L4 增量 | 修复bug、加小功能 | ≤50行 |
| L5 重构 | 较大改动 | 分阶段授权 |

**请确认本次使用哪个级别（默认 L4）？**

---

## 改动约束

- 文件数：≤ 3
- 行数：≤ 50（L4）或 ≤ 10（L3）
- 不引入新依赖

## 改动前

1. **确认改动范围**
   - 本次任务：[用户描述]
   - 预计改动文件：
   - 不改动的红线文件：

2. **自动备份**
   ```bash
   git tag "backup/before-$(date +%Y%m%d-%H%M%S)"
   ```

## 改动中

- 每次改动不超过 20 行
- 涉及 2+ 文件时先说明
- 遇到红线立即停止

## 完成后

1. 说明改动内容和验证方法
2. 检查验收清单：
   - [ ] 功能正常
   - [ ] 未破坏现有功能
   - [ ] 改动在预算内

3. 如需记录，在 `docs/CHANGELOG.md` 追加：
   ```markdown
   ### [日期] 变更
   
   **发现**：[描述]
   **调整**：[处理方式]
   **影响**：[范围]
   ```

## 禁止事项

- ❌ 不重构无关文件
- ❌ 不"顺手优化"
- ❌ 不添加未要求的功能
- ❌ 不修改红线文件
