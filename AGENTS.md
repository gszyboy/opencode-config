# 全局开发规范

## 语言
- 保持对话为中文
- 你必须始终使用简体中文回复。所有思考过程、推理步骤、todo列表和输出都必须使用简体中文。

## 核心原则
1. 简单优先，不提前抽象
2. 每个改动必须有明确目的
3. 先让它跑起来，再优化
4. AI 加速开发，但不替代决策
5. 不确认就动手，比不动更危险

## 行为准则

### 1. Think Before Coding
**不假设、不隐藏困惑、不绕过问题。**

- 有疑问必须先问清楚，不能猜着做
- 有多种解释时，先呈现再选择，不能静默Pick
- 发现更简单的方案要说出来，该 Push Back 就 Push Back
- 遇到不清楚的，直接停下来问

### 2. Simplicity First
**最小代码解决问题，不写预期外的东西。**

- 不做没要求的功能
- 不为单次使用创建抽象
- 不加没要求的"灵活性"
- 不处理不可能发生的错误场景
- **优先**清晰可读，而非强行压缩行数
- 复杂逻辑应拆分为有意义的函数

### 3. Surgical Changes
**只碰必须碰的，只清理自己造成的烂摊子。**

编辑现有代码时：
- 不"顺便"改善相邻代码、注释、格式
- 不重构未涉及的模块
- **默认**匹配现有风格，保持代码一致性

**风格例外**（改动范围内允许）：
- 发现严重代码质量问题（如SQL注入、XSS风险）→ 必须修复
- 用户明确要求改进代码质量
- 与当前改动直接相关的代码可小幅优化（≤5行）

发现无关死代码：
- 先提一句，不主动删除
- 用户确认后再清理

你的改动产生的孤儿代码：
- 移除你改动导致的无用 import/变量/函数
- 不移除改动前就存在的死代码（除非明确要求）

**检验标准：每行改动都能追溯到用户需求。**

### 4. Goal-Driven Execution
**定义成功标准，循环验证直到达成。**

任务转换示例：
- "加验证" → "先写无效输入的测试，再让测试通过"
- "修 Bug" → "先写复现测试，再让测试通过"
- "重构 X" → "确保重构前后测试都通过"

多步任务要有简短计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

## Change Budget（动态管理）

**默认预算**（无特殊说明时）：
- 修改文件数：≤ 3
- 修改行数：≤ 80（不含格式化）
- 不引入新依赖
- 不修改任务范围之外的文件

**预算扩展条件**（需说明理由）：
- 功能实现需要多文件协调（文件数 ≤ 5）
- 复杂逻辑需要更多代码（行数 ≤ 150）
- 新增测试文件不计入主预算（但需适度）

**超出预算处理**：
1. 先完成核心改动（符合预算部分）
2. 说明剩余工作需要的预算
3. 等待用户确认后再继续

## 设计原则（默认遵循，可合理例外）

**简约原则**（默认）：
- 最小代码解决问题，不为单次使用创建抽象
- 不添加"为未来扩展"的代码
- 不过度工程（YAGNI 原则）

**例外情况**（需明确说明理由）：
- 用户明确要求分层架构或设计模式
- 识别到 3+ 处重复代码需要提取
- 项目初始化/架构设计阶段

**重构原则**（默认）：
- 不主动重构未涉及的文件模块
- 单次任务不追求完美，先跑通再优化

**允许情况**（需说明收益）：
- 改动范围内发现严重代码异味（如SQL注入、硬编码密钥）
- 与当前任务直接相关的代码可以顺带优化（≤5行）
- 消除明显的重复代码

**决策原则**：
- 不静默做选择，有歧义必须先问
- 发现更简单的方案要提出，该 Push Back 就 Push Back

## 多模态图像分析工具优先级

当需要分析、解读图片时，按以下优先级选择工具：

**第一优先：look_at**
- look_at 工具(它是oh-my-openagent内置工具)
- 它使用 multimodal-looker agent（支持 mimo-v2.5 / gemini-3.1-pro 等 vision 模型）
- 可直接分析图像内容并返回文字描述

**第二优先：MiniMax_understand_image**
- 直接调用 MiniMax 多模态模型
- 支持图片分析，返回文字描述
- 当 look_at 不可用时使用

**第三优先：filesystem_read_media_file**
- MCP 文件系统工具，仅返回 base64 编码和 MIME 类型
- 当前两个工具都不可用时，可获取图像数据供其他方式处理

## Filesystem MCP 仅在以下情况使用

- 需要批量并发读取多个文件（>3 个）
- 需要读取媒体文件base64数据时
- 项目根目录不在当前工作区，需要访问外部受限目录
- 需要原子写入或 git-style diff 预览功能

## 文档检索优先级 

**首选 Context7**：涉及具体库/框架 API 时调用
**次选 websearch**：需要了解社区最佳实践、GitHub issue 讨论、非官方方案时
**备选 mmx**：minimax CLI 工具

## 安全规范
- ❌ 禁止在代码中硬编码密钥、密码、token
- ❌ 禁止提交 .env 文件到 Git
- 使用环境变量或 {env:VAR_NAME} 语法

### 目录安全
**危险目录禁止操作**：以下目录不适合执行代码操作，发现后必须立即警告用户并拒绝创建/修改任何文件：
- `~` 用户根目录
- `/` 根目录
- `/etc`、`/var`、`/usr`、`/boot`、`/sys`、`/proc`、`/dev` 系统目录

发现用户在危险目录时，必须：
1. 立即通过 TUI toast 或回复提醒用户
2. 拒绝创建、修改任何代码文件
3. 建议用户切换到正规项目目录

## Git 规范
- Commit 信息简洁明了
- 每次 commit 只做一件事
- 提交前检查敏感信息

## 开发节奏
1. 明确任务范围 → **不确定就问**
2. 设置 Change Budget
3. 复杂任务先 Plan Mode 分析
4. 小步快跑，不追求一次到位
5. 完成后简单验证

## 决策升级机制

当任务复杂度超出常规约束时，按以下流程处理：

**Level 1：常规模式**（默认）
- 严格遵循 Change Budget 和各项原则
- 适用于：bug修复、小功能添加、配置调整

**Level 2：扩展模式**（主动申请）
- 适用场景：
  - 需要修改 4-5 个文件
  - 代码量预计 80-150 行
  - 需要新建 service/repository 层
- 操作：说明理由，申请扩展预算，等待确认

**Level 3：架构模式**（需明确授权）
- 适用场景：
  - 项目初始化/架构设计
  - 大规模重构（>150行）
  - 涉及数据库、认证、核心业务改动
- 操作：先输出评估报告，用户授权后再执行

**升级话术模板**：
```
当前任务预计需要：
- 修改 X 个文件（预算：Y 个）
- 新增 Z 行代码（预算：W 行）
- 需要 [具体突破的约束]

建议升级到 Level 2，理由：[具体原因]
是否确认？
```

## 模块化规范（按需加载）

需要具体规范时引用对应文件：

- 通用开发规范：@~/.config/opencode/docs/general-rules.md
- 后端规范：@~/.config/opencode/docs/backend-rules.md
- 前端规范：@~/.config/opencode/docs/frontend-rules.md
- TypeScript 规范：@~/.config/opencode/docs/typescript-rules.md

## 已有项目规范

处理已有项目时引用：

- 已有项目规范：@~/.config/opencode/docs/existing-project-rules.md
- 项目评估命令：/assess-project
- 增量开发命令：/incremental-dev

### 已有项目核心规则

**50行平衡模式**：
- 改动 ≤ 50行
- 文件 ≤ 3个
- 不引入新依赖

**改动红线（绝对不改，除非明确授权）**：
1. 数据库结构/ORM模型
2. 认证鉴权逻辑
3. 支付财务代码
4. 核心业务算法

**介入级别**：
- L1 只读：刚接手、不确定风险
- L2 诊断：定位问题、分析原因
- L3 小改：改一个函数/一行配置（≤10行）
- L4 增量：修复bug、加小功能（≤50行）
- L5 重构：较大改动（分阶段授权）
